name: # name of the machine
machine_category: # category of the machine (e.g., milling, turning, handling)
machine_type: # type of the machine (e.g., vertical, horizontal)
machine_model: # model of the machine
description:
root:
  !!FolderNode
  name: 
  description: 
  children: